import React from "react";

const Input = () => {
  
  return (
    <section>
      <input type="text" placeholder="Digite o nome do pais" />
      <button>Search</button>
    </section>
  );
};

export default Input;
